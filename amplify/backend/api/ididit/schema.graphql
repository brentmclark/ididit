type Goal @model {
  id: ID!
  name: String!
  tasks: [Task] @connection(name: "GoalTasks")
  objective: Int!
}

type Task @model {
  id: ID!
  title: String!
  goal: Goal @connection(name: "GoalTasks")
  value: Int!
}
